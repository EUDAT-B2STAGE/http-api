version: '3'

services:

  postgres:
    environment:
      ACTIVATE: 1
  icat:
    environment:
      ACTIVATE: 0
  iclient:
    environment:
      ACTIVATE: 0

  # sqlalchemyui:
  #   environment:
  #     ACTIVATE: 1
  #   ports:
  #     - 8080:8080
