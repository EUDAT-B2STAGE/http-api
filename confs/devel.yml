version: '3'

services:
  backend:
    volumes:
      # Rapydo repos
      - ../submodules/http-api/restapi:${PYTHON_PATH}/restapi
      - ../submodules/utils/utilities:${PYTHON_PATH}/utilities
      # Unit tests
      - ../submodules/http-api/tests:/code/tests
      - ../projects/${COMPOSE_PROJECT_NAME}/backend/tests:/code/tests/custom

  # celery:
  #   volumes:
  #     - ../submodules/http-api/restapi:${PYTHON_PATH}/restapi
  #     - ../submodules/utils/utilities:${PYTHON_PATH}/utilities