
#######################
# Link services inside API

backend:
  environment:
    DEBUG_LEVEL: VERBOSE
    BACKEND_AUTH_SERVICE: graphdb
  links:
    - gdb:gdb

frontend:
  ports:
    - 80:5000

gdb:
  ports:
  - 9090:7474
