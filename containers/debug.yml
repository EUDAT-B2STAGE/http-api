version: '3'
#######################
# Link services inside API

services:
  backend:
    environment:
      APP_MODE: debug
      DEBUG_LEVEL: VERBOSE
    ports:
      - 8081:5000

  frontend:
    ports:
      - 80:5000
    environment:
      APP_MODE: debug
      BACKEND_PORT: 8081

  neo4j:
    ports:
      - 9090:7474
      - 7687:7687

  swaggerui:
    ports:
      - 7777:80
