
version: '3'
services:

  backend:
    # build: ../../../builds/backend
    # # # Convention: use project/service:SOME_LABEL
    # # # where SOME_LABEL != template, e.g. "custom"
    # image: ${COMPOSE_PROJECT_NAME}/backend:custom

    environment:
      ACTIVATE: 1
      FLASK_DEBUG: 1
      DEBUG_LEVEL: VERY_VERBOSE
      APP_MODE: debug
      # EUDAT RELATED
      B2ACCESS_ENV: development
    ports:
      - 8080:${FLASK_DEFAULT_PORT}
    depends_on:
      ##############################
      # to be commented if you need to test external b2safe
      - icat
      - postgres
      ##############################
      - mongodb
    # volumes:
    #   - ../submodules/prc/irods:/usr/local/lib/python3.6/dist-packages/irods

  restclient:
    environment:
      ACTIVATE: 1
      APP_HOST: ${FLASK_HOST}
      APP_PORT: :${FLASK_DEFAULT_PORT}
    networks:
      app_net:
    depends_on:
      - backend

  # graphdb:
  #   ports:
  #     - 9090:7474
  #     - 7687:7687

  sqlalchemyui:
    # volumes:
    #   - restlitedb:/dbs
    ports:
      - 80:8888
  swaggerui:
    ports:
      - 80:80
  mongoui:
    ports:
      - 80:8081
