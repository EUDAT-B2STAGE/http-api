
# depends_on:
#   - not SEADATA_PROJECT

# The name of the REST class
class: PIDEndpoint
# The python file where your REST class is located
# (inside the 'vanilla/apis/' dir)
file: extended

mapping:
  #all: "/pids"
  mypid: /pids/<path:pid>
ids:
  pid: the EUDAT PID to be resolved
labels:
  - eudat
  - pids
