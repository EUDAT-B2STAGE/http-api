
common:
  custom:
    # FIXME
    authentication: false
    # authorized:
    #   - staff_user

messages:
  summary: Send messages on a queue
  parameters:
    - name: queue
      in: query
      type: string
      description: queue for the message
      default: hello
    - name: message
      in: query
      type: string
      description: message to be sent
      default: "Hello, World!"
  responses:
    '200':
      description: unknown
