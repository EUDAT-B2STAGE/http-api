common:
  custom:
    authentication: true
    # authentication: false

approve:
  summary: Register files that passed QCs into Production namespace
  # parameters:
  #   - name: filelist
  #     in: body
  #     schema:
  #       $ref: "#/definitions/FileList"
  responses:
    '200':
      description: Registration process launched
