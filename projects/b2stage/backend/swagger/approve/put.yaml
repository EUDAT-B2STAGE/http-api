common:
  custom:
    authentication: true
    # authentication: false

approve:
  summary: Register a file from a batch passing all QCs into Production
  # parameters:
  #   - name: filelist
  #     in: body
  #     schema:
  #       $ref: "#/definitions/FileList"
  responses:
    '200':
      description: Registration executed
