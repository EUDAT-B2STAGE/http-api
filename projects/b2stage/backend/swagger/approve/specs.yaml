
depends_on:
  - SEADATA_PROJECT

file: approve
class: MoveToProductionEndpoint
schema:
  expose: true
mapping:
  approve: "/ingestion/<string:batch_id>/approve/<string:temp_id>"
labels:
  - seadatacloud
  # - resources
  - ingestion
