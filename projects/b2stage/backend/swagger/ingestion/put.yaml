
# withid:
withidandfile:
  custom:
    authentication: true
  summary: Upload a zip file to the ingestion cloud
  parameters:
    - name: file
      in: formData
      description: file data to be uploaded
      required: true
      type: file
  responses:
    '200':
      description: File was uploaded without errors
