
withoutid:
  custom:
    authentication: true
  summary: Enable a batch to be ingested in the submission process
  responses:
    '200':
      description: The batch for the specified identifier has been activated
  parameters:
    - name: batch_id
      in: query
      type: string
      description: the identifier associated to this ingestion workflow
