common:
  custom:
    # authentication: false
    authentication: true
    authorized:
      - admin_root
      - staff_user
    required_roles: any

restricted:
  summary: Request to import a zip file into a restricted order
  responses:
    '200':
      description: Request unqueued for download
