depends_on:
  - SEADATA_PROJECT

depends_on:
  - SEADATA_PROJECT

file: restricted
class: Restricted
schema:
  expose: true
mapping:
  restricted: "/restricted/<string:order_id>"
  # upload: "/restricted/<string:order_id>/add/<string:file_id>"
labels:
  - seadatacloud
  - restricted
