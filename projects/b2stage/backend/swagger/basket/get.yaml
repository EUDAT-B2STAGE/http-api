common:
  custom:
    #custom authentication: true
    authentication: false

download:
  summary: "Download an order"
  # parameters:
  #   - name: code  # passed in the URL
  #     in: query
  #     type: string
  #     description: code to verify the order download
  #     # default: hello
  responses:
    '200':
      description: The order with all files compressed
