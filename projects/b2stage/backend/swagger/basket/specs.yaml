
depends_on:
  - SEADATA_PROJECT

file: basket
class: DownloadBasketEndpoint
schema:
  expose: true
mapping:
  # basket: "/orders"
  # order: "/orders/<string:order_id>"
  download: "/orders/<string:order_id>/download/<string:ftype>/c/<string:code>"
labels:
  - seadatacloud
  # - resources
  - order
