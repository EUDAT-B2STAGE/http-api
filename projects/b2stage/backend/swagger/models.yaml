
definitions:

  FileUpdate:
    required:
      - newname
    properties:
      resource:
        type: string
      newname:
        type: string
        description: B2SAFE resource

  FileDelete:
    properties:
      # debugclean:
      #   type: string
      #   description: Only for debug mode
      resource:
        type: string

  # FileObject:
  #   properties:
  #     resource:
  #       type: string
  #       # description: yep

  # ForceObject:
  #   properties:
  #     force:
  #       type: boolean
  #       default: false
