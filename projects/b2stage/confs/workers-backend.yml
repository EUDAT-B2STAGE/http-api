version: '3'

services:

  backend:
    environment:
      ACTIVATE: 0

  rabbit:
    environment:
      ACTIVATE: 1
    ports:
      - 5672:5672

  mongodb:
    environment:
      ACTIVATE: 1
    ports:
      - 27017:27017  

  # redis: