common:
  custom:
    authentication: true
    # authentication: false

approve:
  summary: Approve files in a batch that are passing all QCs
  parameters:
    - name: parameters
      in: body
      schema:
        $ref: "#/definitions/SeadataPost"
  responses:
    '200':
      description: Registration executed
