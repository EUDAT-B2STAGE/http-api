single:
  summary: Add metadata to object
  custom:
    authentication: true
    authorized:
      - normal_user
  parameters:
    - name: metadata
      in: body
      required: true
      schema:
        type: array
        items:
          type: string
  responses:
    '200':
      description: Metadata added