
# LABEL mapped into SPECS.yaml must match inside the METHOD.yaml
# inside METHOD raise error if label does not exist in specs

##################
# common to different alias (like a default)
common:
  parameters:
    - name: resource
      description: just a test
      in: query
      type: string
      # schema:
      #   $ref: "#/definitions/Credentials"
  custom:
    # publish: false
    authentication: true
    authorized:
      - normal_user
      # - admin # what if not matching? should be admin_root
  # tags:
  #   - namespace

##################
# all:
#   summary: this is just a test
#   description: "catch'em all"
#   responses:
#     '200':
#       description: hell yes

single:
  summary: this is just a single test
  responses:
    '200':
      description: "hell yeah!"

##################
# # TO BE REMOVED, just a test
# test:
#   summary: this is just a single test
#   responses:
#     '200':
#       description: "hell yeah!"
