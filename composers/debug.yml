
version: '3'
services:

  rest:
    environment:
      FLASK_DEBUG: 1
      DEBUG_LEVEL: VERY_VERBOSE
      APP_MODE: debug
    networks:
      dbnet:
      inet:
    depends_on:
      - graphdb
      - icat
      # - sql
    ports:
      - 8080:5000
    # volumes:
    #   - restlitedb:/dbs

  # sqladmin:
  #   # volumes:
  #   #   - restlitedb:/dbs
  #   ports:
  #     - 8888:8888

  graphdb:
    ports:
      - 9090:7474

  swagclient:
    ports:
      - 80:80
