
version: '3'
services:

  rest:
    environment:
      APP_MODE: debug
      # DEBUG_LEVEL: VERBOSE
      DEBUG_LEVEL: VERY_VERBOSE
    # volumes:
    #   - restlitedb:/dbs
    ports:
      - 8080:5000

  # graphdb:
  #   ports:
  #     - 9090:7474

# TO FIX
  # sqladmin:
  #   # volumes:
  #   #   - restlitedb:/dbs
  #   ports:
  #     - 8888:8888

  # swagclient:
  #   ports:
  #     - 80:80
