
#############################
# TO FIX: remove this file and `do init` command?
# 
# It will be done when confs/fixirodsclient.sh
# will be mapped to a builds/apiserver file
#############################

version: '3'
services:

#   sql:
#     volumes:
#      - ./confs/pgs_init.sh:/docker-entrypoint-initdb.d/setup-my-schema.sh:ro

#   icat:
#     command: /init
#     # command: sleep infinity    ## DEBUG
#     # volumes:
#     #  - ./confs/credentials.conf:/myconfig:ro

  rest:
    user: root
    # command: sleep infinity    ## DEBUG
    command: /fix
    volumes:
      - ./confs/fixirodsclient.sh:/fix
